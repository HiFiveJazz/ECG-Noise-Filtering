s = tf('s');
% 
% Ro = 50;
% Rc = 16.23;
% C = 200*10^-9;
% a = (10^5) /(1+0.1*s);
% fs = (1/(s*C)) / (Ro+Rc+(1/(s*C)));
% T = a*fs;
% % nyquistplot(T)
% 
% 
% s = tf('s');
% t_A = 0.1;
% A_0 = 10^5;
% C = 200*10^-9;
% a_s = ((A_0)/(1+s*t_A));
% R_0 = 50;
% Rc = 16.23;
% T = (a_s)*((1/(s*C))+Rc)/(R_0+Rc+(1/(s*C)));
% %bode (T,{1e-2,1e6})
s = tf('s');
Ro = 50;
Rc = 16.23;
C = 200*10^-9;
a = (10^5) /(1+0.1*s);
fs = ((1/(s*C)) +Rc) / (Ro+Rc+(1/(s*C)));
T = a*fs;
bode (T,{1e-2,1e6})
nyquistplot(T)

